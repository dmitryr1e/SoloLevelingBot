line-length = 110
target-version = "py312"

[lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = [
    "E501",  # длинные строки допустимы в текстах бота
    "B008",
]

[lint.per-file-ignores]
"tests/*" = ["E402"]  # sys.path-хак до импортов — осознанно
