services:
  bot:
    build: .
    restart: always
    env_file:
      - .env
    environment:
      BOT_DATA_DIR: /app/data
    volumes:
      - bot_data:/app/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"

volumes:
  bot_data:
